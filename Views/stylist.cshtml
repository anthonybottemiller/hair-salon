@{
  Layout="Shared/_Layout.cshtml";
}
    @if (Model.Count == 0)
    {
      <p>This stylist doesn't have any clients yet!</p>
    }
    else
    {
      <ul>
      @foreach (var client in Model)
      {
        <li>@client.GetName()</li>
      }
      </ul>
    }
