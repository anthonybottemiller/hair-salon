@{
  Layout="Shared/_Layout.cshtml";
}
<div class="container">
  <h1>Sandy Hair Salon</h1>
  <div class="col-sm-6">
    @if (Model.Count == 0)
    {
      <p>No stylists are in the system yet!</p>
    }
    else
    {
      <ul>
      @foreach (var stylist in Model)
      {
        <li><a href="/stylist/@stylist.GetId()"</a> @stylist.GetName()</li>
      }
      </ul>
    }
  </div>
</div>
